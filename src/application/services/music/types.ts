export type MusicInfosType = {
  name: string;
};

export type MusicDownloadUrlType = {
  saveToPath: string;
  url: string;
  saveName: string;
};

export type GetLocalMusicStreamType = {
  filePath: string;
  fileName: string;
};
