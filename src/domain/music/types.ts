export type CreateMusicType = {
  name: string;
  artist: string;
  url: string;
};

export type MusicDBType = {
  id: string;
  name: string;
  artist: string;
  url: string;
  createdAt: string;
  updatedAt: string;
};
